extends layout

block content
    .w3-card-4
        header.w3-container.w3-teal
            h3 Aluno: #{student.numero}
        .w3-container

                li Nome: #{student.nome}
                li 
                    Git: a(href=student.git)=student.git
                    ul
                        each tpc, i in student.tpc
                            if(tpc==1)
                                div.tpc
                                    b='TPC ' + (i+1) + ": " 
                                    a= " entregue"
                            else
                                div.tpc
                                     b='TPC ' + (i+1) + ": " 
                                     a= " por entregar"

        footer(class='w3-cotainer w3-teal')
            p(style= "text-align: center;")
                a(href='/students') Voltar à Lista de Alunos  
            address dawApp :: 2020-Nov

